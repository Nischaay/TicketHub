<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SP18.PF.G09.Xamarin.Views.ListVenues">
    <ListView x:Name="VenuesListView">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{Binding name}" TextColor="Black"></Label>
                        <Label Text="{Binding description}" TextColor="Black"></Label>
                        <Label Text="{Binding capacity}" TextColor="Black"></Label>
                        <!--<Label Text="{Bindind PhysicalAddress}"{
                        <Label Text="{Binding addressLine1}" TextColor="Black"></Label>
                        <Label Text="{Binding addressLine2}" TextColor="Black"></Label>
                        <Label Text="{Binding zipCode}" TextColor="Black"></Label>
                        <Label Text="{Binding city}" TextColor="Black"></Label>
                            <Label Text="{Binding state}" TextColor="Black"></Label>
                        }
                        </Label>-->
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>